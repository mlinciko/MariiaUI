import { CellValuePipe } from './cell-value.pipe';

describe('CellValuePipe', () => {
  it('create an instance', () => {
    const pipe = new CellValuePipe();
    expect(pipe).toBeTruthy();
  });
});
