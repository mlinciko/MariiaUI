import { IsSelectedDatePipe } from './is-selected-date.pipe';

describe('IsSelectedDatePipe', () => {
  it('create an instance', () => {
    const pipe = new IsSelectedDatePipe();
    expect(pipe).toBeTruthy();
  });
});
