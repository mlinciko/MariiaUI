import { CheckboxLabelPipe } from './checkbox-label.pipe';

describe('CheckboxLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckboxLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
