import { BaseIcon } from './base-icon.directive';

describe('BaseIcon', () => {
  it('should create an instance', () => {
    const directive = new BaseIcon();
    expect(directive).toBeTruthy();
  });
});
